import { memo } from "react";
import { Services } from "../../components/organisms/services";

const Service = () => {
  return (
    <div>
      <Services />
    </div>
  );
};

export default memo(Service);
